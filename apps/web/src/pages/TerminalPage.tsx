/**
 * Placeholder — an interactive terminal needs a provider with the
 * `terminal` capability, which no Milestone 1 provider offers yet.
 */
export function TerminalPage() {
  return (
    <div className="empty-state">
      <h2>Terminal</h2>
      <p>
        The interactive terminal needs a connected provider with terminal
        support. No available provider offers this yet.
      </p>
      <span className="badge-soon">Not available yet</span>
    </div>
  )
}
