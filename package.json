{
  "name": "pocket-agent",
  "version": "0.1.0",
  "private": true,
<<<<<<< HEAD
  "description": "Pocket Agent - a lightweight local web UI for an AI coding agent, optimized for Android, Termux and Acode.",
  "license": "MIT",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
=======
  "description": "Mobile-first AI coding workspace for Android developers using Termux and Acode.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @pocket-agent/web",
    "build": "npm run build --workspace @pocket-agent/web",
    "preview": "npm run preview --workspace @pocket-agent/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
>>>>>>> main
  }
}
